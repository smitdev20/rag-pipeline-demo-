# Required: API key for OpenAI or any OpenAI-compatible provider
OPENAI_API_KEY=your-key-here

# Optional: use a different API (e.g. Azure, local proxy)
# LLM_BASE_URL=https://your-api.example.com/v1
# LLM_MODEL=gpt-4o-mini

# Server
# LOG_LEVEL=INFO
# HOST=0.0.0.0
# PORT=8000

# NiceGUI (when UI runs with main app)
# NICEGUI_STORAGE_SECRET=rag-chatbot-secret

# UI only: API base URL when NiceGUI talks to FastAPI (e.g. separate processes)
# API_BASE_URL=http://localhost:8000
